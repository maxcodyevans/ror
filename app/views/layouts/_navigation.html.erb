<div class="navbar navbar-default navbar-static-top">
  <div class="container">
    <div class="navbar-header">

      <%= link_to "De-Cabo-A-Rabo", root_path, class: 'navbar-brand' %>

      <div
  class="fb-like"
  data-share="true"
  data-width="450"
  data-show-faces="true">
</div>
    </div>
   
      <ul class="navbar-right">
        <% if !current_user %>
     
          <%= link_to "Vote", user_session_path,  class: 'btn btn-lg btn-success'  %>
        <% else %>
          <li id='show-user'><em><%= current_user.email %></em></li>
          <%= link_to "Logout", destroy_user_session_path, method: :delete, class: 'navbar-link'  %>
          <%= link_to "Votar", edit_user_registration_path,  class: 'btn btn-lg btn-success'  %>
        <% end %>
      </ul>
    
  </div>
</div>
